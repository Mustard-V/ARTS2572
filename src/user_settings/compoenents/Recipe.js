import React from "react";
import { Layout } from "antd";
const Recipe = (props) => {
    const { Content } = Layout;

    return (
        <Content>
            <p>Recipe</p>
        </Content>
    );
};
export default Recipe;
